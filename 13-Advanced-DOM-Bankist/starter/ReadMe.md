<!-- Методът insertAdjacentHTML в JavaScript се използва за
  вмъкване на HTML код в определено място вътре в един елемент,
   без да се замества съществуващото съдържание, а го допълва. -->

<!-- Където element е елементът, в който искате да вмъкнете HTML код, position е позицията, на която ще се извърши вмъкването, и text е HTML кодът, който искате да вмъкнете.

   Стойностите за параметъра position могат да бъдат:

   'beforebegin': Преди самия елемент.
   'afterbegin': Вътре в елемента, в началото на съдържанието му.
   'beforeend': Вътре в елемента, в края на съдържанието му.
   'afterend': След елемента. -->
